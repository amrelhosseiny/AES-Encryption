# AES-Encryption
Advanced Encryption Satndards implemneted in c++


Input: 

The AES Reads input from the console as lines.
first line: no. of cases, then a sesgment that is repeated for every case
segemnt: no. of encryptions in a line then key in a line then plain text in a line

Output:
a line with cyper text for every case
